# Project - Общий код для лабораторных работ

## Установка

```bash
composer install
```

## Тестирование

```bash
make test              # Unit тесты (без БД)
make test-integration  # Integration тесты (требуется MySQL)
make test-all         # Все тесты
# или
./vendor/bin/phpunit
```

**Примечание**: Integration тесты автоматически пропускаются, если БД недоступна.

## Статический анализ

```bash
make phpstan
# или
./vendor/bin/phpstan analyse
```

## Генерация документации

**Простой способ** (автоматически выберет лучший метод):

```bash
make docs
```

Проект автоматически использует:
1. Docker (если доступен и работает)
2. Встроенный PHP генератор (работает всегда, не требует зависимостей)

### Альтернативные методы

```bash
# Встроенный генератор (рекомендуется, если Docker недоступен)
php generate-docs.php

# Или через скрипт
./docs-setup.sh

# Или через Docker (если доступен)
docker pull phpdoc/phpdoc:3
docker run --rm -v $(pwd):/data phpdoc/phpdoc:3 -d src -t docs/html
```

Документация будет доступна в `docs/html/index.html`

**Примечания:** 
- ✅ Встроенный генератор работает с любой версией PHP
- ❌ Глобальная установка через `composer global require` не работает с PHP 8.5+
- 🔧 Проблемы с Docker credentials не критичны - используется альтернативный метод генерации документации
- ✅ Unit тесты работают без БД
- ✅ Integration тесты автоматически пропускаются, если MySQL недоступен

## Все проверки

```bash
make all
```

## Структура

- `src/Security/` - Компоненты безопасности (CSRF)
- `src/Validation/` - Валидаторы данных
- `src/Helpers/` - Вспомогательные классы
- `src/Database/` - Работа с базой данных
- `src/Utils/` - Утилиты (CSV, отчеты)
- `tests/` - Тесты

## Автор

fakedesyncc
